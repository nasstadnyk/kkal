using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Runtime.Serialization.Json;
using System.Text;
using System.Threading.Tasks;

namespace kkal
{
    class Json
    {
       // public static void Save_json_Disk(string path_json1)
        //{
           // Product[] product_json = new Product[Product.Product.Count];
          //  for (int i = 0; i < Product.Product.Count; i++)
            //{
              //  product_json[i] = Product.Product[i];
           // }
            //DataContractJsonSerializer jsonFormatter1 = new DataContractJsonSerializer(typeof(Product[]));
            //using (FileStream fs = new FileStream(path_json1, FileMode.OpenOrCreate))
            //{
              //  jsonFormatter1.WriteObject(fs, product_json);
            //}
       // }
      //  public static void Open_json_Disk(string path_json1)
        //{
          //  DataContractJsonSerializer formatter1 = new DataContractJsonSerializer(typeof(Product[]));
            //using (FileStream fs = new FileStream(path_json1, FileMode.Open))
           // {
             //   Product[] products = (Product[])formatter1.ReadObject(fs);
               // foreach (Product f in products)
                //{
                  //  Product.Product.Add(f);
                    
                }
            }

